specVersion: 0.35.0
name: my-function
runtime: node
build:
  git:
    url: https://github.com/LastKnightXZ/my-function.git   # required, git repository with the function source code
    revision: main                                    # optional, git revision to be used (branch, tag, commit)
    contextDir: myfunction                          # optional, needed only if the function is not located in the repository root folder
  buildpacks: []
  builder: "s2i"
  buildEnvs: []
created: 2024-01-30T15:19:34.422359237+01:00
